

// lib/constants/app_colors.dart

import 'package:flutter/material.dart';

class app_colors {
    // Primary Color
    static const Color primary              = Color(0xFF3F5185); 
    static const Color primary_light        = Color(0xFF757DE8);
    static const Color primary_dark         = Color(0xFF002984);

    // Accent Color 
    static const Color accent               = Color(0xFFFF4081);
    static const Color accent_light         = Color(0xFFFF7980);
    static const Color accent_dark          = Color(0xFFC60055);

    // Background Color
    static const Color background           = Color(0xFFF5F5F5);
    static const Color surface              = Colors.white;

    // Text Color
    static const Color text_primary         = Color(0xFF212121);
    static const Color text_secondary       = Color(0xFF757575);
    static const Color text_hint            = Color(0xFFBDBDBD);

    // Signals Color
    static const Color error                = Color(0xFFD32F2F);
    static const Color success              = Color(0xFF388E3C);
    static const Color warning              = Color(0xFFFFA000);
    static const Color info                 = Color(0xFF197602);

    // Navigation Bar
    static const Color nav_bar_background       = primary;
    static const Color nav_bar_active_item      = accent;
    static const Color nav_bar_inactive_item    = Colors.white70;

    // Divider
    static const Color divider      = Color(0xFFBDBDBD);

    // Card
    static const Color shadow       = Color(0x40000000);
}
